flask==3.0.3
flask-cors==4.0.1
qdrant-client==1.10.1
sentence-transformers==3.1.0
openai==1.54.0
celery==5.4.0
redis==5.0.8
requests==2.32.3
gunicorn==22.0.0
torch==2.4.1+cpu
# torch CPU-only build keeps the Docker image manageable (~1.5 GB instead of 5+ GB)
# If you have a GPU available: replace with torch==2.4.1 (CUDA) in Dockerfile
